# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  # All php-files
  watch(%r{.+\.php})
  # Reload once the css-files get compiled, no need to watch the scss-dir
  watch(%r{public/.+\.(css|html|js)})
end
